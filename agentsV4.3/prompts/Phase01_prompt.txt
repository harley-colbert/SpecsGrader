You are FrontendAgent. Execute **Phase01_UX_ModelSetTopCompact.md** from `planV4.3/` exactly as written.

Objective:
Move Model Set (.sgm) section to the top of Train pane and make it compact; preserve existing behaviors and responsiveness.

Support:
- Coordinate with: OrchestratorAgent, QualityAgent
- Keep changes within the phase scope.

Hard requirements:
- Run and pass: `pytest -q` and manual UI checks (order, compactness, export/import, load hydration)
- If any test fails, fix the root cause and re-run until passing.
- Check off every item in the phase success checklist before declaring done.

Deliverable:
- Updated repo state that satisfies the phase success checklist.
- A short report: changed files list + test commands run + outcomes.
