# Phase 0 — Baseline Screenshots

Screenshots were not captured because the current environment is headless and lacks a runnable display server for the PySide6 GUI. To complete this artifact later:
- Launch `splash.py` (or `app.py`) in a desktop-enabled environment.
- Capture: fresh launch, project/model set loaded, post-classification view (ideally with Needs Review rows), and log tab.
- Save them as `02_ASSETS/baseline_*.png` alongside this note.
